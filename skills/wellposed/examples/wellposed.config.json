{
  "$comment": "Pass with --config. Silence or re-level any rule whose premise does not hold for your data. Rule ids come from `wellposed rules`.",
  "rules": {
    "choice/no-escape-hatch": "warn",
    "request/single-question": "off"
  }
}
